{
  "name": "next-landings",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend-dev": "cd backend && NODE_ENV=production npm run build && npm run develop",
    "frontend-dev": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend-dev\" \"npm run frontend-dev\"",
    "install": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
